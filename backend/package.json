{
  "name": "pizzamaster-pro-backend",
  "version": "2.0.0",
  "description": "Sistema Comercial PizzaMaster Pro — Backend API",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "db:setup": "psql -U postgres -c 'CREATE DATABASE pizzamaster;' && psql -U postgres -d pizzamaster -f ../database/schema.sql",
    "db:reset": "psql -U postgres -c 'DROP DATABASE IF EXISTS pizzamaster;' && npm run db:setup"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "jsonwebtoken": "^9.0.2",
    "pg": "^8.11.3"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "author": "PizzaMaster Pro",
  "license": "MIT"
}
